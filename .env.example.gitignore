# Copy to .env and set values for production
# PORT the app listens on
PORT=3000
# Use 'production' in production
NODE_ENV=production
# Set to true if running behind a trusted reverse proxy (loads balancer)
TRUST_PROXY=false
# Provide a strong random secret for sessions
SESSION_SECRET=replace_with_strong_secret

# Database Configuration (choose one approach):
# Option 1: Use DATABASE_URL connection string for Postgres
# DATABASE_URL=postgresql://user:password@localhost:5432/dbname

# Option 2: Use individual Postgres connection parameters
# DB_CLIENT=postgres
# PG_HOST=localhost
# PG_PORT=5432
# PG_USER=postgres
# PG_PASSWORD=your_password
# PG_DATABASE=dev_mgmt

# Default is SQLite (no env vars needed), uses ./users.db

# Optional Redis for session storage (if not set, uses Knex store)
# REDIS_URL=redis://localhost:6379

# Optional SSL cert/key paths for HTTPS (if you want Node to serve HTTPS)
#SSL_CERT_PATH=/path/to/cert.pem
#SSL_KEY_PATH=/path/to/key.pem

# Stripe (test mode)
# STRIPE_SECRET_KEY=sk_test_...
# STRIPE_PRICE_ID=price_...
